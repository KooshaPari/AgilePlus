{
  "mcpServers": {
    "dispatch-mcp": {
      "command": "dispatch-mcp",
      "cwd": "${workspaceFolder}/dispatch-mcp",
      "disabled": false,
      "alwaysAllow": [
        "dispatch",
        "healthcheck",
        "route"
      ]
    }
  }
}
