version: 0.1

cli:
  version: 1.22.12

plugins:
  sources:
    - id: trunk
      ref: v1.6.6
      uri: https://github.com/trunk-io/plugins

lint:
  enabled:
    - actionlint
    - clippy
    - ruff
    - rustfmt
    - shellcheck
    - yamllint
