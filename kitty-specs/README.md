# kitty-specs/ (relocated)

Eco specs moved to [`docs/specs/eco/`](../docs/specs/eco/). Archived `meta.json` files
live under [`docs/_archive/meta-json/`](../docs/_archive/meta-json/); fields are folded
into each `spec.md` YAML frontmatter.
