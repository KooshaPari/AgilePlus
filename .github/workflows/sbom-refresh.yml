name: Monthly SBOM Refresh
on:
  schedule:
    - cron: '0 0 1 * *'  # 1st of each month
  workflow_dispatch:
permissions:
  contents: read


jobs:
  call-sbom-refresh:
    uses: KooshaPari/PhenoShared/.github/workflows/sbom-monthly.yml@2a65aa09be60fb008a1871dbf175b406ac5ddf45
    with:
      repo_path: '.'
      build_system: 'rust'
