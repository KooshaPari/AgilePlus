name: VitePress Pages

on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - ".github/workflows/deploy.yml"
  workflow_dispatch:

jobs:
  deploy:
    uses: KooshaPari/phenoShared/.github/workflows/vitepress-pages.yml@main
    with:
      concurrency_group: agileplus-vitepress-pages
      build_script: "vitepress build ."
      custom_domain: "true"
    permissions:
      contents: read
      pages: write
      id-token: write
