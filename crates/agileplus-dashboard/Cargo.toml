[package]
name = "agileplus-dashboard"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[package.metadata.askama]
dirs = ["../../templates"]

[dependencies]
agileplus-domain = { path = "../agileplus-domain" }
agileplus-events = { path = "../agileplus-events" }
agileplus-sqlite = { path = "../agileplus-sqlite" }
agileplus-fixtures = { path = "../agileplus-fixtures" }

# Phenotype Health Integration
phenotype-health = { path = "../../../crates/phenotype-health", features = ["http"] }
phenotype-project-registry = { path = "../../../crates/phenotype-project-registry" }
phenotype-compliance-scanner = { path = "../../../crates/phenotype-compliance-scanner" }
phenotype-security-aggregator = { path = "../../../crates/phenotype-security-aggregator", features = ["real-sources"] }
phenotype-health-cli = { path = "../../../crates/phenotype-health-cli" }

askama = "0.12"
axum = { version = "0.8", features = ["json", "macros"] }
axum-extra = { version = "0.10", features = ["typed-header"] }
tower = { version = "0.5" }
tower-http = { version = "0.6", features = ["cors", "trace", "fs"] }
tokio.workspace = true
serde.workspace = true
serde_json.workspace = true
toml = "0.8"
thiserror.workspace = true
anyhow.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
chrono = { version = "0.4", features = ["serde"] }
sysinfo = "0.32"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
