../*
../.*
*.toml
*.yaml
*.json
!pyproject.toml

# Agent worktrees (leaked cross-repo contamination)
.claude/worktrees/
.claude/scheduled_tasks.lock
# Agent / MCP caches — local-only
.serena/
.codex/
.cursor/

# Agent worktrees (leaked cross-repo contamination)
.claude/worktrees/
target/
