# CI Tool Layering Strategy:
# Tier 1 (Critical): cargo clippy/fmt, cargo test - Must pass
# Tier 2 (Important): CodeQL, Semgrep - Important signals
# Tier 3 (Nice-to-have): Snyk, Fossa - Security enrichment (non-blocking)
#
# Snyk is Tier 3 - FAILURE DOES NOT BLOCK MERGE
# Configure at: https://app.snyk.io/org/phenotype/settings/integrations/github-integration

name: Snyk Security Scan

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '0 2 * * *'

permissions:
  contents: read
  security-events: write
  pull-requests: write

jobs:
  snyk-test:
    name: Snyk Vulnerability Test
    runs-on: ubuntu-latest
    # Tier 3: Non-blocking - does not prevent merge
    # Results available at: https://app.snyk.io
    continue-on-error: true  # KEY: Prevents Snyk quota from blocking PRs
    env:
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Snyk CLI
        run: npm install -g snyk

      - name: Authenticate with Snyk
        run: snyk auth "$SNYK_TOKEN"

      - name: Run Snyk test
        id: snyk_test
        continue-on-error: true
        run: |
          snyk test --severity-threshold=high --json-file-output=snyk-test-report.json --all-projects --detection-depth=3

      - name: Generate SARIF report
        if: always()
        continue-on-error: true
        run: |
          snyk test --sarif-file-output=snyk.sarif --severity-threshold=low --all-projects --detection-depth=3 || true

      - name: Upload SARIF to GitHub Code Scanning
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: snyk.sarif
          category: snyk-security-scan

      - name: Add PR comment
        if: github.event_name == 'pull_request' && always()
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '## Snyk Security Scan Results\n\nSnyk vulnerability scan completed. View results in GitHub Code Scanning dashboard.'
            });

      - name: Fail on critical vulnerabilities
        if: steps.snyk_test.outcome == 'failure'
        run: echo "Critical vulnerabilities detected." && exit 1

  snyk-monitor:
    name: Snyk Monitor
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    env:
      SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Monitor dependencies
        run: |
          npm install -g snyk
          snyk auth "$SNYK_TOKEN"
          snyk monitor --all-projects --detection-depth=3 || true
