# Force LF line endings in the repository for all text files.
# This prevents CRLF/LF divergence between Windows dev machines and Linux CI.
* text=auto eol=lf

# Explicitly binary files - no line ending conversion
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.wasm binary
